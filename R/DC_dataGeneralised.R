#'
#' Data check dataGeneralised Chech if dataGeneralizations is a NULL column (ie, latitude and longitude were not generalized)
#'
#'     This data check answers: "?" question.\cr Data check will pass if \strong{Latitude and longitude values have not been generalized as indicated by dwc:dataGeneralizations is NULL} and will fail if \strong{Latitude and longitude values may have been generalized as indicated by dwc:dataGeneralizations is not NULL}.\cr Dimension of this data check is \strong{Resolution} and it's flagging type is: \strong{FLAG}\cr Example of entries that will pass: \code{}, such data check would return \code{}.\cr Example of entries that will fail: \code{}, such data check would return \code{}.
#' @name dataGeneralised
#' @docType data
#' @format An object of class \code{"dataCheck"}, see \code{\link{dataCheck}} for details.
#' @references LATER

NULL
